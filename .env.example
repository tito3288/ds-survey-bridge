# Exact application origin used to create private survey-token links. Do not
# include credentials, a path, query, or fragment. Production must use HTTPS;
# local HTTP is allowed only for localhost or 127.0.0.1.
APP_URL=http://localhost:3000

# Supabase server connection. Never expose the service-role key to a browser.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:55321
SUPABASE_SERVICE_ROLE_KEY=replace-with-local-or-hosted-service-role-key

# Existing SMS delivery configuration.
TWILIO_ACCOUNT_SID=AC_replace_me
TWILIO_AUTH_TOKEN=replace_me
TWILIO_PHONE_NUMBER=+15555550100

# Durable delivery configuration. Zero makes a new survey text immediately
# eligible for the next worker run. The worker refuses to claim jobs unless it
# is explicitly enabled in its private Railway service.
SURVEY_SMS_DELAY_MINUTES=0
DELIVERY_WORKER_ENABLED=false

# Non-public Batch 6 operational commands. Each command refuses to run unless
# its own switch is explicitly enabled in the isolated service that owns it.
# Health reports aggregate queue/provider state. Reconciliation checks Twilio
# messages accepted at least 12 hours ago; it never resends them. Cleanup
# deletes only minimal callback receipts older than 90 days.
DELIVERY_HEALTH_ENABLED=false
DELIVERY_RECONCILER_ENABLED=false
DELIVERY_EVENT_CLEANUP_ENABLED=false

# Private-questionnaire email configuration. RESEND_API_KEY belongs only on the
# delivery worker. Separate SUPPORT_EMAIL recipients with commas.
RESEND_API_KEY=re_replace_me
# The web app needs this signing secret to verify exact raw callback bodies; it
# does not need the send-capable RESEND_API_KEY for callback verification.
RESEND_WEBHOOK_SECRET=whsec_replace_me
SUPPORT_EMAIL=support@driveandshine.com

# Active overall-rating threshold. Ratings at or above this value use the
# location's mapped Google review page; lower ratings use private feedback.
# Must be a whole number from 1 through 5. Default: 4.
GOOGLE_REVIEW_MIN_RATING=4
